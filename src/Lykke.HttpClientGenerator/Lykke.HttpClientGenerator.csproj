<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <Version>5.6.2</Version>
    <PackageLicenseUrl>
      https://github.com/LykkeBusinessPlatform/Lykke.HttpClientGenerator/blob/master/LICENSE
    </PackageLicenseUrl>
    <PackageProjectUrl>
      https://github.com/LykkeBusinessPlatform/Lykke.HttpClientGenerator
    </PackageProjectUrl>
    <Description>
      This package helps to generate client proxy for an api using the Refit library.
      It adds cache and retries using Polly to the generated proxies.
      User logic can also be added as wrappers to the requests or to the method calls.
    </Description>
    <PackageTags>refit;polly;dotnet-core;csharp;api-client</PackageTags>
    <PackageId>LykkeBiz.HttpClientGenerator</PackageId>
    <IncludeSourceRevisionInInformationalVersion>false</IncludeSourceRevisionInInformationalVersion>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="Lykke.Snow.Common" Version="4.0.2" />
    <PackageReference Include="LykkeBiz.Common" Version="8.3.0" />
    <PackageReference Include="Nito.AsyncEx" Version="5.1.2" />
    <PackageReference Include="Polly" Version="7.2.3" />
    <PackageReference Include="Refit" Version="5.2.4" />
  </ItemGroup>
</Project>